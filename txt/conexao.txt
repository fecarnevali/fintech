<?php

  $hostname = "localhost";
  $bancodedados = "clientes";
  $usuario = "root";
  $senha = "";

  $mysqli = new mysqli($hostname, $usuario, $senha, $bancodedados);
  if ($mysqli->connect_errno)
  {
    echo "Falha ao conectar" . $mysqli->connect_errno . ") " . $mysqli->connect_error;
  }

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $email = $_POST['email'];
    $senha = $_POST['senha'];

    // Realizar validações e consultas no banco de dados
    try {
        $stmt = $pdo->prepare("SELECT * FROM contas WHERE email = :email AND senha = :senha");
        $stmt->bindParam(':email', $email);
        $stmt->bindParam(':senha', $senha);
        $stmt->execute();
        
        if ($stmt->rowCount() == 1) {
            // Login bem-sucedido
            // Redirecionar para a página de perfil ou outra página após o login
            header("Location: perfil.php");
            exit();
        } else {
            // Login falhou
            echo "Credenciais inválidas.";
        }
    } catch (PDOException $e) {
        echo "Erro: " . $e->getMessage();
    }
}
if ($_SERVER["REQUEST_METHOD"] == "POST") {
    $nome = $_POST['nome'];
    $email_criar = $_POST['email_criar'];
    $cpf = $_POST['cpf'];
    $senha_criar = $_POST['senha_criar'];

    // Realizar validações e inserção no banco de dados
    try {
        $stmt = $pdo->prepare("INSERT INTO contas (nome, email, cpf, senha) VALUES (:nome, :email, :cpf, :senha)");
        $stmt->bindParam(':nome', $nome);
        $stmt->bindParam(':email', $email_criar);
        $stmt->bindParam(':cpf', $cpf);
        $stmt->bindParam(':senha', $senha_criar);
        $stmt->execute();

        // Criação de conta bem-sucedida
        // Você pode redirecionar para uma página de sucesso ou fazer outras ações
        echo "Conta criada com sucesso!";
    } catch (PDOException $e) {
        echo "Erro: " . $e->getMessage();
    }
}
?>



// Consulta preparada para evitar SQL Injection
        $query = "SELECT * FROM contas WHERE email = ?";
        $stmt = $mysqli->prepare($query);
        $stmt->bind_param("s", $email);
        $stmt->execute();
        $result = $stmt->get_result();

        $senha_criar = password_hash($_POST['senha'], PASSWORD_DEFAULT); // Hash de senha segura

        .container {
    display: flex;
    justify-content: flex-end;
    align-items: center;
    height: 100%;
    padding: 20px;
}